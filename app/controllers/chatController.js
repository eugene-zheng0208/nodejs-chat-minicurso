var path = require('path');

exports.index = function(req, res) {
	// TODO: Se o usuário não estiver logado, envia o usuário para a página de login, caso esteja logado, mostra o html do chat
}

exports.logout = function(req, res) {
	// TODO: Se o usuário estiver logado, destrói a seção e envia ele para a página de login
}
