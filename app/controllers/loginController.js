var path = require('path');

exports.login = function(req, res) {
	// TODO: Caso o usuário já esteja logado, redireciona para o chat, caso não esteja exibe o formulário de login
}

exports.loginPost = function(req, res) {
	// TODO: Se o usuário digitar o nome, atribui o nome do usuário a seção e envia ele para o chat, senão envia o usuário para a página de login novamente
}