// TODO: Carregue os módulos express, app e bodyParser

// TODO: Carregue a seção e as rotas

/*
// Utilizando o método que irá interpretar os formulários
app.use(bodyParser.urlencoded());
app.use(bodyParser.json());

// Aplica as seções
app.use(session);

// Utilizando as rotas
app.use('/', routes);

// Configurações de arquivos estáticos
app.use('/public', express.static('public'));
app.use('/node_modules', express.static('node_modules'));
*/

module.exports = app;